export type TestCompoProp = {
  coolname: string;
  youngage: string;
};

export type NavBarProp = {
  endpoint: string;
};

export type ProductType = {
  id: number;
  name: string;
  description: string;
  categories: string[];
  variants: string[];
  size: string;
};
